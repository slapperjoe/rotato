<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Applied only to MainActivity. Makes the window fully transparent and
        removes all enter/exit animations so tapping the launcher icon shows
        nothing — the service just starts silently in the background.
    -->
    <style name="Theme.Rotato.Transparent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
